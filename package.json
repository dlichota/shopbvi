{
  "name": "larder-flow",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first pantry tracker PWA backed by a Google Sheet",
  "type": "module",
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "test": "node --experimental-strip-types --test \"test/*.test.mts\" \"test/*.test.mjs\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@netlify/functions": "^5.3.0",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
